Task: Parse a Kubernetes manifest and validate required fields

Python Topic: YAML parsing
Kubernetes Task: Manifest validation
Use Case: Ensure all manifests include apiVersion, kind, and metadata.name before deployment.