# Python script to convert .env to ConfigMap

🔧 Use Case
You have a .env file with environment variables, and you want to convert it into a Kubernetes ConfigMap YAML using Python. This is useful for automating deployments and managing configuration cleanly.

Task: Create a Kubernetes ConfigMap from a .env file

Python Topic: File I/O, string parsing
Kubernetes Task: ConfigMap generation
Use Case: Convert environment variables into a deployable ConfigMap